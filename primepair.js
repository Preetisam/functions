(function(){
    let prime = true;
    function isprime(num){
        for(let i= 2; i < Math.sq(num); i++){
            if(num % i ==0){
                prime = false;
                break;
            }
        }
        return prime;
    }
    function is prime(num){
        for (let i= )
    }
})();

